# Google Maps key: Places (search) + Geocoding (“Use current” GPS → city).
# Copy to `.env` (gitignored). Enable Places API + Geocoding API; restrict to your app bundle IDs.
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=
