/**
 * Connections feature area — typography + repeated surfaces.
 * Headings: TS Block Bold. Body / UI chrome: Helvetica Neue (no Arial drift).
 */

export const FONT_HEADING = "TS Block Bold";
export const FONT_BODY = "Helvetica Neue";

/** Dark rounded card used for message rows, discover cards, skeleton tiles, etc. */
export const cardTile = {
  backgroundColor: "hsl(0, 0%, 8%)",
  borderRadius: 16,
  borderWidth: 1,
  borderColor: "hsl(0, 0%, 15%)",
};
