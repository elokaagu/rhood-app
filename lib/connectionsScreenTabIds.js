/**
 * Tab keys for Connections / Discover screen (header, navigation, data hooks).
 * Prefer these over raw strings to avoid typos across the connections module.
 */
export const CONNECTIONS_SCREEN_TAB = Object.freeze({
  DISCOVER: "discover",
  CONNECTIONS: "connections",
});
