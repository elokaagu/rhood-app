/** SectionList section discriminator for connections tab (intent, not only React `key`). */
export const CONNECTIONS_SECTION_TYPE = Object.freeze({
  COMMUNITY: "community",
  CONNECTION: "connection",
});

/** @deprecated Use CONNECTIONS_SECTION_TYPE */
export const CONNECTION_SECTION_TYPE = CONNECTIONS_SECTION_TYPE;
